@inherits LayoutComponentBase
<div class="flex w-full flex-col min-h-screen bg-zinc-950">
    <TopBar />

    <main class="flex flex-col flex-1">
        <article class="flex-1 px-2 w-full">
            @Body
        </article>

        <Footer />
    </main>
</div>
